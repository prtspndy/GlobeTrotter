# GlobeTrotter client

This directory contains the React and Vite frontend for GlobeTrotter.

## Commands

Run these commands from the repository root:

```bash
npm run client:dev
npm run client:build
npm run client:lint
```

The client reads its API endpoint from `VITE_API_BASE_URL`. Copy
`.env.example` to `.env` before starting the application.
